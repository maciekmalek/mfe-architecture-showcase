{
  "name": "shared-components",
  "sourceRoot": "src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest"
      }
    }
  }
}
