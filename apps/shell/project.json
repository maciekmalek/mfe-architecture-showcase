{
  "name": "shell",
  "sourceRoot": ".",
  "projectType": "application",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next dev -p 3000"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next build"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next lint"
      }
    }
  }
}
